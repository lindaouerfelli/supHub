
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/suphub_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: lilipg
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8090


springdoc:
  default-produces-media-type: application/json



